---
classes:
  - role::docker_host

firewalld::ports:
  'Plex':
    ensure: present
    zone: public
    port: 32400
    protocol: 'tcp'

docker::run_instance::instance:
  plex:
    image: 'linuxserver:plex'
    net: 'host'
    env:
      - 'VERSION:latest' 
